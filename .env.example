# AWS Bedrock – native IAM auth (never commit .env).
# Set IAM credentials, or use: aws configure
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-west-2

# PostgreSQL – shared by API and all agents (read/write). Prefer PG* (libpq); agents also accept DATABASE_URL or RDS*.
# PGHOST=your-db-host.elb.us-east-1.amazonaws.com
# PGUSER=app
# PGPASSWORD=
# PGDATABASE=hackathon
# PGPORT=5432

# Or use DATABASE_URL (full URI) or RDS* (RDSHOST + RDS_USER + RDS_PASSWORD); optional RDS_DB (default: postgres).
DATABASE_URL=
RDSHOST=team2-postgresql-db.ca5og0yikblq.us-east-1.rds.amazonaws.com
RDS_USER=app_user
RDS_PASSWORD=
# RDS_DB=postgres

# Sureify API (Option A – recommended for testing). Shared by API server and agents (agent_one, agent_two).
# Option A: SUREIFY_BASE_URL + SUREIFY_BEARER_TOKEN (no token endpoint call).
# Option B: SUREIFY_BASE_URL + SUREIFY_CLIENT_ID + SUREIFY_CLIENT_SECRET (client_credentials).
# When unset, agents use mock data. Endpoints: /puddle/policyData, /puddle/productOption, /puddle/suitabilityData, /puddle/clientProfile.
SUREIFY_BASE_URL=https://hackathon-dev-api.sureify.com
SUREIFY_BEARER_TOKEN=
# Option B only:
# SUREIFY_TOKEN_URL=https://hackathon-dev-sureify.auth.us-west-2.amazoncognito.com/oauth2/token
# SUREIFY_CLIENT_ID=
# SUREIFY_CLIENT_SECRET=
# SUREIFY_SCOPE=hackathon-dev-EdgeApiM2M/edge

# IRI Annuity Renewal Intelligence API (optional – for get_iri_alerts, snooze, dismiss, dashboard/stats)
# IRI_API_BASE_URL=http://localhost:3000/api
# IRI_API_BASE_URL=https://api.iri-hackathon.com/api

# API server URL (agents call passthrough + alerts endpoints)
API_BASE_URL=http://localhost:8000

# Agents service URL (API calls agents for product comparison)
AGENTS_URL=http://localhost:8001
