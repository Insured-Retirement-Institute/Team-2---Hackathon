# AWS Bedrock – native IAM auth (never commit .env).
# Set IAM credentials, or use: aws configure
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-west-2

# RDS PostgreSQL (get password from workshop dashboard). AgentTwo reads DB for context + recommendations.
# Use DATABASE_URL or (RDSHOST + RDS_USER + RDS_PASSWORD); optional RDS_DB (default: postgres).
DATABASE_URL=
RDSHOST=team2-postgresql-db.ca5og0yikblq.us-east-1.rds.amazonaws.com
RDS_USER=app_user
RDS_PASSWORD=
# RDS_DB=postgres

# Sureify API – shared by API and agentOne (api.sureify_client). When set, agent uses get_policies / get_notes.
# When unset, agentOne uses mock data for Marty McFly.
# SUREIFY_BASE_URL=https://hackathon-dev-api.sureify.com
# SUREIFY_CLIENT_ID=
# SUREIFY_CLIENT_SECRET=
# SUREIFY_TOKEN_URL=  (optional; default hackathon Cognito)
# SUREIFY_SCOPE=      (optional; default hackathon-dev-EdgeApiM2M/edge)

# IRI Annuity Renewal Intelligence API (optional – for get_iri_alerts, snooze, dismiss, dashboard/stats)
# IRI_API_BASE_URL=http://localhost:3000/api
# IRI_API_BASE_URL=https://api.iri-hackathon.com/api
